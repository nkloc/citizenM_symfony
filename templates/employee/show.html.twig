{% extends 'base.html.twig' %}

{% block title %}Employee{% endblock %}

{% block body %}
    <h1>Employee</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ employee.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ employee.Firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ employee.Lastname }}</td>
            </tr>
            <tr>
                <th>Adress</th>
                <td>{{ employee.adress }}</td>
            </tr>
            <tr>
                <th>Iban</th>
                <td>{{ employee.iban }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary white" href="{{ path('employee_index') }}">back to list</a>

    <a class="btn btn-primary white" href="{{ path('employee_edit', {'id': employee.id}) }}">edit</a>

    {{ include('employee/_delete_form.html.twig') }}
{% endblock %}
