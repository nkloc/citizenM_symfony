{% extends 'back/base.html.twig' %}

{% block title %}Edit SchoolClass{% endblock %}

{% block body %}
    <h1>Edit SchoolClass</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}

    <a href="{{ path('back_school_class_index') }}">back to list</a>

    {{ include('back/school_class/_delete_form.html.twig') }}
{% endblock %}
