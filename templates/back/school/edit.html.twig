{% extends 'back/base.html.twig' %}

{% block title %}Edit School{% endblock %}

{% block body %}
    <h1>Edit School</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}

    <a href="{{ path('back_school_index') }}">back to list</a>

    {{ include('back/school/_delete_form.html.twig') }}
{% endblock %}
