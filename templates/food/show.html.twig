{% extends 'base.html.twig' %}

{% block title %}Food{% endblock %}

{% block body %}
    <h1>Food</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ food.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ food.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ food.price }}</td>
            </tr>
            <tr>
                <th>Hôtels</th>
                <td>
                    {% for hotel in food.hotels %}
                        <span class="badge bg-danger p-2" data-badge-caption="">{{ hotel.name }}</span>
                    {% endfor %}
                </td>
            </tr>

        </tbody>
    </table>

    <a class="btn btn-primary white" href="{{ path('food_index') }}">back to list</a>
   {% if is_granted('ROLE_ADMIN') %}
        <a class="btn btn-primary white" href="{{ path('food_edit', {'id': food.id}) }}">edit</a>
        {{ include('food/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
