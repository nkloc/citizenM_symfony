{% extends 'base.html.twig' %}

{% block title %}Client{% endblock %}

{% block body %}
    <h1>Client</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ client.Firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ client.Lastname }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ client.email }}</td>
            </tr>
            <tr>
                <th>Hôtels</th>
                <td>
                    {% for hotel in client.hotels %}
                        <span class="badge bg-danger p-2" data-badge-caption="">{{ hotel.name }}</span>
                    {% endfor %}
                </td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary white" href="{{ path('client_index') }}">back to list</a>
   {% if is_granted('ROLE_ADMIN') %}
        <a class="btn btn-primary white" href="{{ path('client_edit', {'id': client.id}) }}">edit</a>
        {{ include('client/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
